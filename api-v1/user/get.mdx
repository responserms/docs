---
openapi: GET /user
---

The Current User endpoint allowed returning the currently authenticated Response user's profile information. This is useful
when building integrations that connect via OAuth and need to know the identity of the user that authorized the token.